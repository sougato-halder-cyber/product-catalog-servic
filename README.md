# Product Catalog Microservice

Final project: REST API (Flask) + unit tests + BDD (behave).

## Run locally
```
pip install -r requirements.txt
flask --app service run        # service on :5000
python -m unittest discover tests
```

## BDD
```
behave features/
```
